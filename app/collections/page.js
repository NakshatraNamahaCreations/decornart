import CollectionsView from "@/components/CollectionsView/CollectionsView";

export const metadata = {
  title: "Collections — Decor N Art",
  description:
    "Browse curated collections of craft materials — pipe cleaners, ribbons, wrapping sheets, flower gift boxes and more.",
};

export default function Page() {
  return <CollectionsView />;
}
