import AddressesView from "@/components/AddressesView/AddressesView";

export const metadata = {
  title: "My Addresses — Decor N Art",
  description:
    "Manage your saved delivery addresses — add, edit or remove home, office and gift-recipient locations.",
};

export default function AddressesPage() {
  return <AddressesView />;
}
